name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        id: snake-gif
        with:
          github_user_name: Alishba-Alvi
          outputs: |
            dist/github-snake.svg?color_snake=%233a1848&color_dots=eee4f0,ffc2d1,ff6f96,e0483f,ffcf6b
            dist/github-snake-dark.svg?color_snake=%233a1848&color_dots=eee4f0,ffc2d1,ff6f96,e0483f,ffcf6b

      - uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
